{% set map = salt['grains.filter_by']({

   'CentOS': {
     'service': 'syncthing@',
   },

   'Debian': {
     'service': 'syncthing@',
   },

}, grain='os', merge=salt['pillar.get']('syncthing:lookup')) %}
